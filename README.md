# Archived: Moved to `multicodex`

This repository is archived and no longer maintained.

Use [`multicodex`](https://github.com/olliecrow/multicodex) instead.

The usage monitor was merged into `multicodex` as `multicodex monitor`.

Do not start new usage-monitor setups from this repository.
